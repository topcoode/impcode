package main

func main() {

	// days := []string{"monday", "tuesday", "wednesday", "thusday", "friday", "saturday", "sunday"}
	// fmt.Println(days)
	// for i := 0; i <= len(days); i++ {
	// 	fmt.Println(days[i])

	// }

	// for _ , i := range days {
	// 	fmt.Println(days[i])
	// }
	// 	fmt.Printf("index is %v and value is %v\n", index, days)
	// }
	// for _ , apple := range days {
	// 	fmt.println("SNO: \t & value: \n", apple)
}
